import { EmployeeOvertimeHour } from './employee-overtime-hour';

describe('EmployeeOvertimeHour', () => {
  it('should create an instance', () => {
    expect(new EmployeeOvertimeHour()).toBeTruthy();
  });
});
